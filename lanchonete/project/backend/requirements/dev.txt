# Inclui tudo do base
-r base.txt

# ── Testes ───────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0          # cliente HTTP para testes

# ── Qualidade de código (opcional) ───────────────────────────
ruff==0.4.4            # linter + formatter ultrarrápido
mypy==1.10.0           # checagem de tipos
